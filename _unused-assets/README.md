Assets that no source file references any more.

Moved here rather than deleted so nothing is lost. They are outside `public/`,
so they are not built, not deployed, and not uploaded to the host.

If a page turns out to need one, move it back into `public/` at the same
relative path. If nothing here is missed after a release, the folder can go.
